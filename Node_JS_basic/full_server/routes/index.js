/**
 * Routes - Définition des routes de l'application
 * 
 * À faire:
 * 1. Importer Express Router
 * 2. Importer AppController et StudentsController
 * 3. Définir les routes:
 *    - '/' -> AppController.getHomepage
 *    - '/students' -> StudentsController.getAllStudents
 *    - '/students/:major' -> StudentsController.getAllStudentsByMajor
 * 4. Exporter le router
 * 
 * Concepts à explorer:
 * - Express Router pour organiser les routes
 * - Paramètres d'URL dynamiques (:major)
 * - Comment séparer les routes de la logique métier
 * - Organisation d'un projet Express
 * 
 * Note: Les routes sont séparées des contrôleurs pour une meilleure organisation
 */
